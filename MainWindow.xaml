<Window
    x:Class="RestXMLTranslator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RestXMLTranslator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="RestXMLTranslator - Перевод"
    Width="1243"
    Height="720"
    FontFamily="Segoe UI, Noto Sans, Arial, Times New Roman"
    PreviewKeyDown="Window_PreviewKeyDown"
    mc:Ignorable="d">
    <Window.Resources>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasChanges}" Value="True">
                    <Setter Property="Background" Value="Yellow" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsApproved}" Value="True">
                    <Setter Property="Background" Value="LightGreen" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="#E6E6E6" />
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="BorderBrush" Value="#4A90E2" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ToggleButton">
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="Background" Value="#EAEAEA" />
            <Setter Property="BorderBrush" Value="#CFCFCF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#D6E8FF" />
                    <Setter Property="BorderBrush" Value="#4A90E2" />
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3F3F3" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  Grid панели управления файлами  -->
        <Border
            Grid.Row="0"
            Margin="5"
            Background="#FAFAFA"
            BorderBrush="#D6D6D6"
            BorderThickness="1"
            CornerRadius="4">
            <ListBox
                x:Name="FilesList"
                Background="Transparent"
                BorderThickness="0"
                ItemsSource="{Binding Files}"
                Loaded="FilesList_Loaded"
                PreviewMouseWheel="FilesList_PreviewMouseWheel"
                ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                SelectedItem="{Binding SelectedFile}"
                SelectionChanged="FilesList_SelectionChanged"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.ScrollUnit="Pixel">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            MaxWidth="160"
                            Text="{Binding Name}"
                            TextTrimming="CharacterEllipsis" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <!--  StackPanel панели управления переводами  -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  ACTIONS  -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">

                <Button
                    x:Name="Save"
                    MinWidth="160"
                    Padding="5,0,5,0"
                    Background="#4A90E2"
                    BorderBrush="#3B78C2"
                    Click="Save_Click"
                    Content="Отправить изменения"
                    FontWeight="SemiBold"
                    Foreground="White" />

                <Button
                    x:Name="LoadTranslation"
                    MinWidth="160"
                    Margin="10,0,10,0"
                    Padding="5,0,5,0"
                    Click="LoadTranslation_Click"
                    Content="Загрузить перевод из файла" />

                <Button
                    x:Name="LoadBufferTranslation"
                    MinWidth="160"
                    Padding="5,0,5,0"
                    Click="LoadBufferTranslation_Click"
                    Content="Загрузить перевод из буфера" />

            </StackPanel>

            <!--  FILTERS  -->
            <StackPanel
                Grid.Column="1"
                Margin="30,0,30,0"
                VerticalAlignment="Center"
                Orientation="Horizontal">

                <ToggleButton
                    Name="HideApproved"
                    Checked="FilterChanged"
                    Content="Скрыть утверждённые"
                    Unchecked="FilterChanged" />

                <ToggleButton
                    Name="HideUnchanged"
                    Margin="10,0,10,0"
                    Checked="FilterChanged"
                    Content="Скрыть неизменённые"
                    Unchecked="FilterChanged" />

                <ToggleButton
                    Name="HideChanged"
                    Checked="FilterChanged"
                    Content="Скрыть изменённые"
                    Unchecked="FilterChanged" />

            </StackPanel>
            <!--  SEARCH  -->
            <Grid Grid.Column="2">
                <TextBox
                    x:Name="SearchBox"
                    Padding="26,0,6,0"
                    VerticalContentAlignment="Center"
                    TextChanged="SearchBox_TextChanged" />

                <TextBlock
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="Gray"
                    IsHitTestVisible="False"
                    Text="🔍" />

                <TextBlock
                    Margin="26,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="Gray"
                    IsHitTestVisible="False"
                    Text="Поиск...">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=SearchBox}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>
        <DataGrid
            x:Name="Grid"
            Grid.Row="2"
            Margin="5,0,5,0"
            AutoGenerateColumns="False"
            BeginningEdit="Grid_BeginningEdit"
            CanUserAddRows="False"
            CanUserSortColumns="True"
            SelectionMode="Extended"
            SelectionUnit="FullRow">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="LightBlue" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="LightBlue" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black" />
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="220"
                    Binding="{Binding Id}"
                    Header="ID"
                    IsReadOnly="True"
                    SortMemberPath="Id" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding Ru}"
                    Header="Русский"
                    IsReadOnly="True"
                    SortMemberPath="Ru" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding Eng}"
                    Header="Английский"
                    IsReadOnly="True"
                    SortMemberPath="Eng" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding NewRu, UpdateSourceTrigger=PropertyChanged}"
                    Header="Новый русский"
                    SortMemberPath="NewRu">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <EventSetter Event="PreviewMouseDoubleClick" Handler="EditLongText" />
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding NewEng, UpdateSourceTrigger=PropertyChanged}"
                    Header="Новый английский"
                    SortMemberPath="NewEng">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <EventSetter Event="PreviewMouseDoubleClick" Handler="EditLongText" />
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn
                    Width="110"
                    Header="Статус"
                    SortMemberPath="IsApproved">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="Approve" Click="Approve_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Content" Value="✔ Утвердить" />
                                        <Setter Property="ToolTip" Value="Утвердить перевод" />

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsApproved}" Value="True">
                                                <Setter Property="Content" Value="✖ Снять" />
                                                <Setter Property="ToolTip" Value="Снять утверждение" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
