<UserControl
    x:Class="RestXMLTranslator.UserControls.FileExplorer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RestXMLTranslator.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0"
            Height="30"
            MinWidth="150"
            Margin="5,5,5,0"
            HorizontalAlignment="Stretch">
            <TextBox
                x:Name="SearchBox"
                Padding="20,0,5,0"
                VerticalContentAlignment="Center"
                TextChanged="SearchBox_TextChanged" />
            <TextBlock
                Margin="5,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Foreground="Gray"
                IsHitTestVisible="False"
                Text="🔍" />
            <TextBlock
                x:Name="SearchPlaceholder"
                Margin="25,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Foreground="Gray"
                IsHitTestVisible="False"
                Text="{Binding Source={StaticResource Locale}, Path=[search_placeholder]}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=SearchBox}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ToggleButton
                x:Name="HideChanged"
                Grid.Row="0"
                Grid.Column="0"
                Height="30"
                Margin="5,5,5,0"
                Padding="5,0,5,0"
                Checked="FileFilterChanged"
                Content="{Binding Source={StaticResource Locale}, Path=[hide_changed]}"
                Unchecked="FileFilterChanged" />
            <ToggleButton
                x:Name="HideUnchanged"
                Grid.Row="0"
                Grid.Column="1"
                Height="30"
                Margin="5,5,5,0"
                Padding="5,0,5,0"
                Checked="FileFilterChanged"
                Content="{Binding Source={StaticResource Locale}, Path=[hide_unchanged]}"
                Unchecked="FileFilterChanged" />
            <ToggleButton
                x:Name="HideFinished"
                Grid.Row="1"
                Grid.Column="0"
                Height="30"
                Margin="5,5,5,5"
                Padding="5,0,5,0"
                Checked="FileFilterChanged"
                Content="{Binding Source={StaticResource Locale}, Path=[hide_finished]}"
                Unchecked="FileFilterChanged" />
            <ToggleButton
                x:Name="HideUnfinished"
                Grid.Row="1"
                Grid.Column="1"
                Height="30"
                Margin="5,5,5,5"
                Padding="5,0,5,0"
                Checked="FileFilterChanged"
                Content="{Binding Source={StaticResource Locale}, Path=[hide_unfinished]}"
                Unchecked="FileFilterChanged" />
        </Grid>
        <ListBox
            x:Name="FilesList"
            Grid.Row="2"
            Margin="5"
            ItemsSource="{Binding Files}"
            ScrollViewer.HorizontalScrollBarVisibility="Visible"
            ScrollViewer.VerticalScrollBarVisibility="Hidden"
            SelectionChanged="FilesList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Click="MenuItem_Click" Header="{Binding Source={StaticResource Locale}, Path=[open_file]}" />
                            </ContextMenu>
                        </Grid.ContextMenu>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="📄" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Finished}" Value="True">
                                            <Setter Property="Text" Value="✔" />
                                            <Setter Property="Foreground" Value="LimeGreen" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock
                            Grid.Column="1"
                            Text="{Binding Name}"
                            TextTrimming="CharacterEllipsis"
                            ToolTip="{Binding RelativePath}"
                            ToolTipService.InitialShowDelay="100" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
